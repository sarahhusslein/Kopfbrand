import React from 'react';
import styles from './finalBar.module.css';

const FinalBar: React.FC = () => {
    return (
        <div className={styles.bar}>
            <p className={styles.text}>KREATIVAGENTUR. HANDWERK. WOW-EFFEKT.</p>
        </div>
    );
};

export default FinalBar;
